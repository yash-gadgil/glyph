[package]
name = "order_book"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
tonic = "0.12"
prost = "0.13"
uuid = { version = "1", features = ["v4"] }

[build-dependencies]
tonic-build = "0.12"
